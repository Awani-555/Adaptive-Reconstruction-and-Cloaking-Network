# Core dependencies
Flask==3.0.3
numpy==1.26.4
opencv-python==4.10.0.84

# Optional but recommended (for threading stability & WSGI serving)
Werkzeug==3.0.2
gunicorn==23.0.0

# Dev utilities (optional but useful)
flask-cors==4.0.1
